# ===========================================
# CreatorFlow 测试环境配置 (Staging)
# ===========================================

# 环境标识
VITE_APP_ENV=staging

# ===========================================
# 云端 API 配置
# ===========================================

# 测试环境 API URL
# 注意：后端服务绑定在 localhost，不能使用局域网 IP
VITE_CLOUD_API_URL=http://localhost:8020/api/v1

# LLM 网关路径
VITE_LLM_GATEWAY_PATH=/llm/proxy

# ===========================================
# 功能开关
# ===========================================

# 启用云端模式
VITE_CLOUD_MODE_ENABLED=true

# 显示调试信息
VITE_DEBUG_MODE=true
